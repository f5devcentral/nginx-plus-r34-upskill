---
title: Forward Proxy Support for E&V Demo
description: Instructions for configuring a forward proxy for telemetry transmission
order: 2
---

## Introduction

This demo illustrates configuring NGINX Plus R34 to transmit E&V data to F5
through an existing forward proxy. 


![NGINX Plus Forward Proxy Support for E&V](media/demo/1.png)

## Demo Lab

This demo video will show the following parts:
1. Configuring NGINX Plus to use an existing forward proxy for E&V transmission
1. Confirm success by viewing the output of the NGINX Plus logs

<br/>

Click on the link below which will take you to the demo video.

[Demo: NGINX R34 Forward Proxy Support forE&V](https://f5u.csod.com/REPLACE-WITH-ViDEO-LINK)

> You must be logged into [LearnF5](https://f5u.csod.com/) to access it.


## Configuration details

In this demo, a forward proxy is configured at nginx-forward-proxy.lab.f5npi,
listening on port 8888.

Add the following line to the mgmt{} section of
/etc/nginx.conf:

```shell
   proxy nginx-forward-proxy.lab.f5npi.net:8888;
```
Restart NGINX Plus R34:

```shell
systemctl restart nginx
```

Confirm success in the nginx-plus error log:

```shell
tail /var/log/nginx/error.log
```

Example output:

```shell
2025/03/24 14:07:06 [error] 1063#1063: proxy returned 400 during usage report to product.connect.nginx.com (via 10.1.1.9:8888)
```

## Up Next

In addition to the forward proxy use case, SSL certificate caching for the proxy
will be demonstrated in the next section.
